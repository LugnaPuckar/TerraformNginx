# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore ALL in /cloud_secrets/
/cloud_secrets/*.txt
/cloud_secrets/
TerraformNginx\cloud_secrets\azure_subscription_id.txt
TerraformNginx\cloud_secrets\gcp_project_id.txt

# Ignore all .tfstate files and .lock.hcl files
cloudplatforms/aws/terraform.tfstate.backup
cloudplatforms/azure/terraform.tfstate.backup
cloudplatforms/gcp/terraform.tfstate.backup
cloudplatforms/azure/terraform.tfstate
cloudplatforms/gcp/terraform.tfstate
cloudplatforms/gcp/.terraform.lock.hcl
cloudplatforms/azure/.terraform.lock.hcl
cloudplatforms/aws/terraform.tfstate
cloudplatforms/aws/.terraform.lock.hcl
cloudplatforms/gcp/terraform.tfstate
cloudplatforms/azure/terraform.tfstate
cloudplatforms/aws/terraform.tfstate
